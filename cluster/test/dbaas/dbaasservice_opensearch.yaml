apiVersion: dbaas.exoscale.m.exoscale.ch/v1alpha1
kind: DBAASService
metadata:
  name: opensearch-${data.suffix}
  namespace: default
spec:
  forProvider:
    name: opensearch-${data.suffix}
    maintenanceDow: sunday
    maintenanceTime: 15:00:00
    terminationProtection: false
    opensearch:
      ipFilter:
        - "0.0.0.0/0"
    plan: hobbyist-2
    type: opensearch
    zone: ${data.zone}
  writeConnectionSecretToRef:
    name: opensearch-${data.suffix}
