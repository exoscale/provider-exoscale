apiVersion: dbaas.exoscale.m.exoscale.ch/v1alpha1
kind: DBAASService
metadata:
  name: valkey-${data.suffix}
  namespace: default
spec:
  forProvider:
    name: valkey-${data.suffix}
    maintenanceDow: sunday
    maintenanceTime: 15:00:00
    terminationProtection: false
    valkey:
      ipFilter:
        - "0.0.0.0/0"
    plan: hobbyist-2
    type: valkey
    zone: ${data.zone}
  writeConnectionSecretToRef:
    name: valkey-${data.suffix}
