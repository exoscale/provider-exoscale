apiVersion: compute.exoscale.m.exoscale.ch/v1alpha1
kind: PrivateNetwork
metadata:
  name: private-network-${data.suffix}
  namespace: default
spec:
  forProvider:
    name: private-network-${data.suffix}
    zone: ${data.zone}
