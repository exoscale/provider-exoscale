apiVersion: compute.exoscale.m.exoscale.ch/v1alpha1
kind: BlockStorageVolume
metadata:
  name: block-storage-${data.suffix}
  namespace: default
spec:
  forProvider:
    name: block-storage-${data.suffix}
    size: 20
    zone: ${data.zone}
