apiVersion: compute.exoscale.m.exoscale.ch/v1alpha1
kind: NLB
metadata:
  annotations:
    meta.upbound.io/example-id: compute/v1alpha1/nlb
  labels:
    testing.upbound.io/example-name: my_nlb
  name: my-nlb
  namespace: crossplane-system
spec:
  forProvider:
    name: my-nlb
    zone: ch-gva-2
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
