---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: sksclusters.compute.exoscale.exoscale.ch
spec:
  group: compute.exoscale.exoscale.ch
  names:
    categories:
    - crossplane
    - managed
    - exoscale
    kind: SKSCluster
    listKind: SKSClusterList
    plural: sksclusters
    singular: skscluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SKSCluster is the Schema for the SKSClusters API. Manage Exoscale
          Scalable Kubernetes Service (SKS) https://community.exoscale.com/product/compute/containers/
          Clusters.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SKSClusterSpec defines the desired state of SKSCluster
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addons:
                    description: (Set of String, Deprecated)
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  audit:
                    description: |-
                      (Block List, Max: 1) Parameters for Kubernetes Audit configuration (may only be enabled at creation time) (see below for nested schema)
                      Parameters for Kubernetes Audit configuration (may only be enabled at creation time)
                    items:
                      properties:
                        bearerTokenSecretRef:
                          description: |-
                            (String, Sensitive) The optional bearer token to include in the request header
                            The optional bearer token to include in the request header
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        enabled:
                          description: |-
                            (Boolean) Whether to run the APIServer with the configured Kubernetes Audit
                            Whether to run the APIServer with the configured Kubernetes Audit
                          type: boolean
                        endpoint:
                          description: |-
                            (String) The cluster API endpoint.
                            The Endpoint URL for the Webserver responsible of processing Audit events
                          type: string
                        initialBackoff:
                          description: |-
                            (String) The Initial Backoff to wait before sending data to the remote server (default '10s')
                            The Initial Backoff to wait before sending data to the remote server (default '10s')
                          type: string
                      type: object
                    type: array
                  autoUpgrade:
                    description: |-
                      (Boolean) Enable automatic upgrading of the control plane version.
                      Enable automatic upgrading of the control plane version.
                    type: boolean
                  cni:
                    description: |-
                      (String) The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                      The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                    type: string
                  description:
                    description: |-
                      form text describing the cluster.
                      A free-form text describing the cluster.
                    type: string
                  enableKarpenter:
                    description: |-
                      (Boolean) Indicates whether to deploy Karpenter for cluster autoscaling.
                      Indicates whether to deploy Karpenter for cluster autoscaling.
                    type: boolean
                  enableKubeProxy:
                    description: |-
                      (Boolean) ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                      ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                    type: boolean
                  exoscaleCcm:
                    description: |-
                      (Boolean) Deploy the Exoscale Cloud Controller Manager in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the Exoscale [Cloud Controller Manager](https://github.com/exoscale/exoscale-cloud-controller-manager/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  exoscaleCsi:
                    description: |-
                      (Boolean) Deploy the Exoscale Container Storage Interface on worker nodes (boolean; default: false; requires the CCM to be enabled).
                      Deploy the Exoscale [Container Storage Interface](https://github.com/exoscale/exoscale-csi-driver/) on worker nodes (boolean; default: `false`; requires the CCM to be enabled).
                    type: boolean
                  featureGates:
                    description: |-
                      (Set of String) Feature gates options for the cluster.
                      Feature gates options for the cluster.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A map of key/value labels.
                      A map of key/value labels.
                    type: object
                    x-kubernetes-map-type: granular
                  metricsServer:
                    description: |-
                      (Boolean) Deploy the Kubernetes Metrics Server in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the [Kubernetes Metrics Server](https://github.com/kubernetes-sigs/metrics-server/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  name:
                    description: |-
                      (String) The SKS cluster name.
                      The SKS cluster name.
                    type: string
                  oidc:
                    description: |-
                      (Block List, Max: 1) An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below. (see below for nested schema)
                      An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) The OpenID client ID.
                            The OpenID client ID.
                          type: string
                        groupsClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user's group.
                            An OpenID JWT claim to use as the user's group.
                          type: string
                        groupsPrefix:
                          description: |-
                            (String) An OpenID prefix prepended to group claims.
                            An OpenID prefix prepended to group claims.
                          type: string
                        issuerUrl:
                          description: |-
                            (String) The OpenID provider URL.
                            The OpenID provider URL.
                          type: string
                        requiredClaim:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) A map of key/value pairs that describes a required claim in the OpenID Token.
                            A map of key/value pairs that describes a required claim in the OpenID Token.
                          type: object
                          x-kubernetes-map-type: granular
                        usernameClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user name.
                            An OpenID JWT claim to use as the user name.
                          type: string
                        usernamePrefix:
                          description: |-
                            (String) An OpenID prefix prepended to username claims.
                            An OpenID prefix prepended to username claims.
                          type: string
                      type: object
                    type: array
                  serviceLevel:
                    description: |-
                      (String) The service level of the control plane (pro or starter; default: pro; may only be set at creation time).
                      The service level of the control plane (`pro` or `starter`; default: `pro`; may only be set at creation time).
                    type: string
                  version:
                    description: |-
                      (String) The version of the control plane (default: latest version available from the API; see exo compute sks versions for reference; may only be set at creation time).
                      The version of the control plane (default: latest version available from the API; see `exo compute sks versions` for reference; may only be set at creation time).
                    type: string
                  zone:
                    description: |-
                      (String) ❗ The Exoscale Zone name.
                      ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/) name.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: (Set of String, Deprecated)
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  audit:
                    description: |-
                      (Block List, Max: 1) Parameters for Kubernetes Audit configuration (may only be enabled at creation time) (see below for nested schema)
                      Parameters for Kubernetes Audit configuration (may only be enabled at creation time)
                    items:
                      properties:
                        bearerTokenSecretRef:
                          description: |-
                            (String, Sensitive) The optional bearer token to include in the request header
                            The optional bearer token to include in the request header
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                        enabled:
                          description: |-
                            (Boolean) Whether to run the APIServer with the configured Kubernetes Audit
                            Whether to run the APIServer with the configured Kubernetes Audit
                          type: boolean
                        endpoint:
                          description: |-
                            (String) The cluster API endpoint.
                            The Endpoint URL for the Webserver responsible of processing Audit events
                          type: string
                        initialBackoff:
                          description: |-
                            (String) The Initial Backoff to wait before sending data to the remote server (default '10s')
                            The Initial Backoff to wait before sending data to the remote server (default '10s')
                          type: string
                      type: object
                    type: array
                  autoUpgrade:
                    description: |-
                      (Boolean) Enable automatic upgrading of the control plane version.
                      Enable automatic upgrading of the control plane version.
                    type: boolean
                  cni:
                    description: |-
                      (String) The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                      The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                    type: string
                  description:
                    description: |-
                      form text describing the cluster.
                      A free-form text describing the cluster.
                    type: string
                  enableKarpenter:
                    description: |-
                      (Boolean) Indicates whether to deploy Karpenter for cluster autoscaling.
                      Indicates whether to deploy Karpenter for cluster autoscaling.
                    type: boolean
                  enableKubeProxy:
                    description: |-
                      (Boolean) ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                      ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                    type: boolean
                  exoscaleCcm:
                    description: |-
                      (Boolean) Deploy the Exoscale Cloud Controller Manager in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the Exoscale [Cloud Controller Manager](https://github.com/exoscale/exoscale-cloud-controller-manager/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  exoscaleCsi:
                    description: |-
                      (Boolean) Deploy the Exoscale Container Storage Interface on worker nodes (boolean; default: false; requires the CCM to be enabled).
                      Deploy the Exoscale [Container Storage Interface](https://github.com/exoscale/exoscale-csi-driver/) on worker nodes (boolean; default: `false`; requires the CCM to be enabled).
                    type: boolean
                  featureGates:
                    description: |-
                      (Set of String) Feature gates options for the cluster.
                      Feature gates options for the cluster.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A map of key/value labels.
                      A map of key/value labels.
                    type: object
                    x-kubernetes-map-type: granular
                  metricsServer:
                    description: |-
                      (Boolean) Deploy the Kubernetes Metrics Server in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the [Kubernetes Metrics Server](https://github.com/kubernetes-sigs/metrics-server/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  name:
                    description: |-
                      (String) The SKS cluster name.
                      The SKS cluster name.
                    type: string
                  oidc:
                    description: |-
                      (Block List, Max: 1) An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below. (see below for nested schema)
                      An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) The OpenID client ID.
                            The OpenID client ID.
                          type: string
                        groupsClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user's group.
                            An OpenID JWT claim to use as the user's group.
                          type: string
                        groupsPrefix:
                          description: |-
                            (String) An OpenID prefix prepended to group claims.
                            An OpenID prefix prepended to group claims.
                          type: string
                        issuerUrl:
                          description: |-
                            (String) The OpenID provider URL.
                            The OpenID provider URL.
                          type: string
                        requiredClaim:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) A map of key/value pairs that describes a required claim in the OpenID Token.
                            A map of key/value pairs that describes a required claim in the OpenID Token.
                          type: object
                          x-kubernetes-map-type: granular
                        usernameClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user name.
                            An OpenID JWT claim to use as the user name.
                          type: string
                        usernamePrefix:
                          description: |-
                            (String) An OpenID prefix prepended to username claims.
                            An OpenID prefix prepended to username claims.
                          type: string
                      type: object
                    type: array
                  serviceLevel:
                    description: |-
                      (String) The service level of the control plane (pro or starter; default: pro; may only be set at creation time).
                      The service level of the control plane (`pro` or `starter`; default: `pro`; may only be set at creation time).
                    type: string
                  version:
                    description: |-
                      (String) The version of the control plane (default: latest version available from the API; see exo compute sks versions for reference; may only be set at creation time).
                      The version of the control plane (default: latest version available from the API; see `exo compute sks versions` for reference; may only be set at creation time).
                    type: string
                  zone:
                    description: |-
                      (String) ❗ The Exoscale Zone name.
                      ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/) name.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.zone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zone)
                || (has(self.initProvider) && has(self.initProvider.zone))'
          status:
            description: SKSClusterStatus defines the observed state of SKSCluster.
            properties:
              atProvider:
                properties:
                  addons:
                    description: (Set of String, Deprecated)
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  audit:
                    description: |-
                      (Block List, Max: 1) Parameters for Kubernetes Audit configuration (may only be enabled at creation time) (see below for nested schema)
                      Parameters for Kubernetes Audit configuration (may only be enabled at creation time)
                    items:
                      properties:
                        enabled:
                          description: |-
                            (Boolean) Whether to run the APIServer with the configured Kubernetes Audit
                            Whether to run the APIServer with the configured Kubernetes Audit
                          type: boolean
                        endpoint:
                          description: |-
                            (String) The cluster API endpoint.
                            The Endpoint URL for the Webserver responsible of processing Audit events
                          type: string
                        initialBackoff:
                          description: |-
                            (String) The Initial Backoff to wait before sending data to the remote server (default '10s')
                            The Initial Backoff to wait before sending data to the remote server (default '10s')
                          type: string
                      type: object
                    type: array
                  autoUpgrade:
                    description: |-
                      (Boolean) Enable automatic upgrading of the control plane version.
                      Enable automatic upgrading of the control plane version.
                    type: boolean
                  cni:
                    description: |-
                      (String) The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                      The CNI plugin that is to be used. Available options are "calico" or "cilium". Defaults to "calico". Setting empty string will result in a cluster with no CNI.
                    type: string
                  createdAt:
                    description: |-
                      (String) The cluster creation date.
                      The cluster creation date.
                    type: string
                  description:
                    description: |-
                      form text describing the cluster.
                      A free-form text describing the cluster.
                    type: string
                  enableKarpenter:
                    description: |-
                      (Boolean) Indicates whether to deploy Karpenter for cluster autoscaling.
                      Indicates whether to deploy Karpenter for cluster autoscaling.
                    type: boolean
                  enableKubeProxy:
                    description: |-
                      (Boolean) ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                      ❗ Indicates whether to deploy the Kubernetes network proxy. (may only be set at creation time)
                    type: boolean
                  endpoint:
                    description: |-
                      (String) The cluster API endpoint.
                      The cluster API endpoint.
                    type: string
                  exoscaleCcm:
                    description: |-
                      (Boolean) Deploy the Exoscale Cloud Controller Manager in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the Exoscale [Cloud Controller Manager](https://github.com/exoscale/exoscale-cloud-controller-manager/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  exoscaleCsi:
                    description: |-
                      (Boolean) Deploy the Exoscale Container Storage Interface on worker nodes (boolean; default: false; requires the CCM to be enabled).
                      Deploy the Exoscale [Container Storage Interface](https://github.com/exoscale/exoscale-csi-driver/) on worker nodes (boolean; default: `false`; requires the CCM to be enabled).
                    type: boolean
                  featureGates:
                    description: |-
                      (Set of String) Feature gates options for the cluster.
                      Feature gates options for the cluster.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  id:
                    description: (String) The SKS cluster ID.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) A map of key/value labels.
                      A map of key/value labels.
                    type: object
                    x-kubernetes-map-type: granular
                  metricsServer:
                    description: |-
                      (Boolean) Deploy the Kubernetes Metrics Server in the control plane (boolean; default: true; may only be set at creation time).
                      Deploy the [Kubernetes Metrics Server](https://github.com/kubernetes-sigs/metrics-server/) in the control plane (boolean; default: `true`; may only be set at creation time).
                    type: boolean
                  name:
                    description: |-
                      (String) The SKS cluster name.
                      The SKS cluster name.
                    type: string
                  nodepools:
                    description: |-
                      (Set of String) The list of exoscale_sks_nodepool (IDs) attached to the cluster.
                      The list of [exoscale_sks_nodepool](./sks_nodepool.md) (IDs) attached to the cluster.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  oidc:
                    description: |-
                      (Block List, Max: 1) An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below. (see below for nested schema)
                      An OpenID Connect configuration to provide to the Kubernetes API server (may only be set at creation time). Structure is documented below.
                    items:
                      properties:
                        clientId:
                          description: |-
                            (String) The OpenID client ID.
                            The OpenID client ID.
                          type: string
                        groupsClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user's group.
                            An OpenID JWT claim to use as the user's group.
                          type: string
                        groupsPrefix:
                          description: |-
                            (String) An OpenID prefix prepended to group claims.
                            An OpenID prefix prepended to group claims.
                          type: string
                        issuerUrl:
                          description: |-
                            (String) The OpenID provider URL.
                            The OpenID provider URL.
                          type: string
                        requiredClaim:
                          additionalProperties:
                            type: string
                          description: |-
                            (Map of String) A map of key/value pairs that describes a required claim in the OpenID Token.
                            A map of key/value pairs that describes a required claim in the OpenID Token.
                          type: object
                          x-kubernetes-map-type: granular
                        usernameClaim:
                          description: |-
                            (String) An OpenID JWT claim to use as the user name.
                            An OpenID JWT claim to use as the user name.
                          type: string
                        usernamePrefix:
                          description: |-
                            (String) An OpenID prefix prepended to username claims.
                            An OpenID prefix prepended to username claims.
                          type: string
                      type: object
                    type: array
                  serviceLevel:
                    description: |-
                      (String) The service level of the control plane (pro or starter; default: pro; may only be set at creation time).
                      The service level of the control plane (`pro` or `starter`; default: `pro`; may only be set at creation time).
                    type: string
                  state:
                    description: |-
                      (String) The cluster state.
                      The cluster state.
                    type: string
                  version:
                    description: |-
                      (String) The version of the control plane (default: latest version available from the API; see exo compute sks versions for reference; may only be set at creation time).
                      The version of the control plane (default: latest version available from the API; see `exo compute sks versions` for reference; may only be set at creation time).
                    type: string
                  zone:
                    description: |-
                      (String) ❗ The Exoscale Zone name.
                      ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/) name.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
