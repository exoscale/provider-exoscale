---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dbaasservices.dbaas.exoscale.exoscale.ch
spec:
  group: dbaas.exoscale.exoscale.ch
  names:
    categories:
    - crossplane
    - managed
    - exoscale
    kind: DBAASService
    listKind: DBAASServiceList
    plural: dbaasservices
    singular: dbaasservice
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DBAASService is the Schema for the DBAASServices API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DBAASServiceSpec defines the desired state of DBAASService
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  grafana:
                    properties:
                      grafanaSettings:
                        description: Grafana configuration settings in JSON format
                          (`exo dbaas type show grafana --settings=grafana` for reference).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  kafka:
                    properties:
                      enableCertAuth:
                        description: Enable certificate-based authentication method.
                        type: boolean
                      enableKafkaConnect:
                        description: Enable Kafka Connect.
                        type: boolean
                      enableKafkaRest:
                        description: Enable Kafka REST.
                        type: boolean
                      enableSaslAuth:
                        description: Enable SASL-based authentication method.
                        type: boolean
                      enableSchemaRegistry:
                        description: Enable Schema Registry.
                        type: boolean
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      kafkaConnectSettings:
                        description: Kafka Connect configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=kafka-connect`
                          for reference).
                        type: string
                      kafkaRestSettings:
                        description: Kafka REST configuration settings in JSON format
                          (`exo dbaas type show kafka --settings=kafka-rest` for reference).
                        type: string
                      kafkaSettings:
                        description: Kafka configuration settings in JSON format (`exo
                          dbaas type show kafka --settings=kafka` for reference).
                        type: string
                      schemaRegistrySettings:
                        description: Schema Registry configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=schema-registry`
                          for reference)
                        type: string
                      version:
                        description: Kafka major version (`exo dbaas type show kafka`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  maintenanceDow:
                    description: The day of week to perform the automated database
                      service maintenance (`never`, `monday`, `tuesday`, `wednesday`,
                      `thursday`, `friday`, `saturday`, `sunday`).
                    type: string
                  maintenanceTime:
                    description: The time of day to perform the automated database
                      service maintenance (`HH:MM:SS`)
                    type: string
                  mysql:
                    properties:
                      adminPasswordSecretRef:
                        description: A custom administrator account password (may
                          only be set at creation time).
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      mysqlSettings:
                        description: MySQL configuration settings in JSON format (`exo
                          dbaas type show mysql --settings=mysql` for reference).
                        type: string
                      version:
                        description: MySQL major version (`exo dbaas type show mysql`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  name:
                    description: ❗ The name of the database service.
                    type: string
                  opensearch:
                    properties:
                      dashboards:
                        properties:
                          enabled:
                            description: 'Enable or disable OpenSearch Dashboards
                              (default: true).'
                            type: boolean
                          maxOldSpaceSize:
                            description: 'Limits the maximum amount of memory (in
                              MiB) the OpenSearch Dashboards process can use. This
                              sets the max_old_space_size option of the nodejs running
                              the OpenSearch Dashboards. Note: the memory reserved
                              by OpenSearch Dashboards is not available for OpenSearch.
                              (default: 128).'
                            type: number
                          requestTimeout:
                            description: 'Timeout in milliseconds for requests made
                              by OpenSearch Dashboards towards OpenSearch (default:
                              30000)'
                            type: number
                        type: object
                      forkFromService:
                        description: ❗ Service name
                        type: string
                      indexPattern:
                        items:
                          properties:
                            maxIndexCount:
                              description: Maximum number of indexes to keep before
                                deleting the oldest one (Minimum value is `0`)
                              type: number
                            pattern:
                              description: fnmatch pattern
                              type: string
                            sortingAlgorithm:
                              description: '`alphabetical` or `creation_date`.'
                              type: string
                          type: object
                        type: array
                      indexTemplate:
                        properties:
                          mappingNestedObjectsLimit:
                            description: The maximum number of nested JSON objects
                              that a single document can contain across all nested
                              types. This limit helps to prevent out of memory errors
                              when a document contains too many nested objects. (Default
                              is 10000. Minimum value is `0`, maximum value is `100000`.)
                            type: number
                          numberOfReplicas:
                            description: The number of replicas each primary shard
                              has. (Minimum value is `0`, maximum value is `29`)
                            type: number
                          numberOfShards:
                            description: The number of primary shards that an index
                              should have. (Minimum value is `1`, maximum value is
                              `1024`.)
                            type: number
                        type: object
                      ipFilter:
                        description: Allow incoming connections from this list of
                          CIDR address block, e.g. `["10.20.0.0/16"]
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      keepIndexRefreshInterval:
                        description: Aiven automation resets index.refresh_interval
                          to default value for every index to be sure that indices
                          are always visible to search. If it doesn't fit your case,
                          you can disable this by setting up this flag to true.
                        type: boolean
                      maxIndexCount:
                        description: Maximum number of indexes to keep (Minimum value
                          is `0`)
                        type: number
                      recoveryBackupName:
                        description: ❗ Name of a backup to recover from
                        type: string
                      settings:
                        description: 'OpenSearch-specific settings, in json. e.g.`jsonencode({thread_pool_search_size:
                          64})`. Use `exo x get-dbaas-settings-opensearch` to get
                          a list of available settings.'
                        type: string
                      version:
                        description: ❗ OpenSearch major version (`exo dbaas type show
                          opensearch` for reference)
                        type: string
                    type: object
                  pg:
                    properties:
                      adminPasswordSecretRef:
                        description: A custom administrator account password (may
                          only be set at creation time).
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      pgSettings:
                        description: PostgreSQL configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pg` for reference).
                        type: string
                      pgbouncerSettings:
                        description: PgBouncer configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pgbouncer` for reference).
                        type: string
                      pglookoutSettings:
                        description: pglookout configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pglookout` for reference).
                        type: string
                      recoveryBackupTime:
                        description: ISO time of a backup to recover from. May only
                          be set at creation time.
                        type: string
                      sharedBuffersPercentage:
                        description: Percentage of total RAM that the database server
                          uses for shared memory buffers. Valid range is 20-60, which
                          corresponds to 20% - 60%. This setting adjusts the shared_buffers
                          configuration value.
                        type: number
                      timescaledbSettings:
                        description: TimescaleDB extension configuration settings
                          in JSON format (`exo dbaas type show pg --settings=timescaledb`
                          for reference).
                        type: string
                      variant:
                        description: PostgreSQL variant (`timescale` or `aiven`).
                          May only be set at creation time.
                        type: string
                      version:
                        description: PostgreSQL major version (`exo dbaas type show
                          pg` for reference; may only be set at creation time).
                        type: string
                      workMem:
                        description: Sets the maximum amount of memory to be used
                          by a query operation (such as a sort or hash table) before
                          writing to temporary disk files, in MB. Default is 1MB +
                          0.075% of total RAM (up to 32MB).
                        type: number
                    type: object
                  plan:
                    description: The plan of the database service (use the [Exoscale
                      CLI](https://github.com/exoscale/cli/) - `exo dbaas type show
                      <TYPE> --plans` - for reference).
                    type: string
                  terminationProtection:
                    description: The database service protection boolean flag against
                      termination/power-off.
                    type: boolean
                  type:
                    description: ❗ The type of the database service (`kafka`, `mysql`,
                      `opensearch`, `pg`, `valkey`, `grafana`).
                    type: string
                  valkey:
                    properties:
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      valkeySettings:
                        description: Valkey configuration settings in JSON format
                          (`exo dbaas type show valkey --settings=valkey` for reference).
                        type: string
                    type: object
                  zone:
                    description: ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/)
                      name.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  grafana:
                    properties:
                      grafanaSettings:
                        description: Grafana configuration settings in JSON format
                          (`exo dbaas type show grafana --settings=grafana` for reference).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  kafka:
                    properties:
                      enableCertAuth:
                        description: Enable certificate-based authentication method.
                        type: boolean
                      enableKafkaConnect:
                        description: Enable Kafka Connect.
                        type: boolean
                      enableKafkaRest:
                        description: Enable Kafka REST.
                        type: boolean
                      enableSaslAuth:
                        description: Enable SASL-based authentication method.
                        type: boolean
                      enableSchemaRegistry:
                        description: Enable Schema Registry.
                        type: boolean
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      kafkaConnectSettings:
                        description: Kafka Connect configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=kafka-connect`
                          for reference).
                        type: string
                      kafkaRestSettings:
                        description: Kafka REST configuration settings in JSON format
                          (`exo dbaas type show kafka --settings=kafka-rest` for reference).
                        type: string
                      kafkaSettings:
                        description: Kafka configuration settings in JSON format (`exo
                          dbaas type show kafka --settings=kafka` for reference).
                        type: string
                      schemaRegistrySettings:
                        description: Schema Registry configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=schema-registry`
                          for reference)
                        type: string
                      version:
                        description: Kafka major version (`exo dbaas type show kafka`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  maintenanceDow:
                    description: The day of week to perform the automated database
                      service maintenance (`never`, `monday`, `tuesday`, `wednesday`,
                      `thursday`, `friday`, `saturday`, `sunday`).
                    type: string
                  maintenanceTime:
                    description: The time of day to perform the automated database
                      service maintenance (`HH:MM:SS`)
                    type: string
                  mysql:
                    properties:
                      adminPasswordSecretRef:
                        description: A custom administrator account password (may
                          only be set at creation time).
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      mysqlSettings:
                        description: MySQL configuration settings in JSON format (`exo
                          dbaas type show mysql --settings=mysql` for reference).
                        type: string
                      version:
                        description: MySQL major version (`exo dbaas type show mysql`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  name:
                    description: ❗ The name of the database service.
                    type: string
                  opensearch:
                    properties:
                      dashboards:
                        properties:
                          enabled:
                            description: 'Enable or disable OpenSearch Dashboards
                              (default: true).'
                            type: boolean
                          maxOldSpaceSize:
                            description: 'Limits the maximum amount of memory (in
                              MiB) the OpenSearch Dashboards process can use. This
                              sets the max_old_space_size option of the nodejs running
                              the OpenSearch Dashboards. Note: the memory reserved
                              by OpenSearch Dashboards is not available for OpenSearch.
                              (default: 128).'
                            type: number
                          requestTimeout:
                            description: 'Timeout in milliseconds for requests made
                              by OpenSearch Dashboards towards OpenSearch (default:
                              30000)'
                            type: number
                        type: object
                      forkFromService:
                        description: ❗ Service name
                        type: string
                      indexPattern:
                        items:
                          properties:
                            maxIndexCount:
                              description: Maximum number of indexes to keep before
                                deleting the oldest one (Minimum value is `0`)
                              type: number
                            pattern:
                              description: fnmatch pattern
                              type: string
                            sortingAlgorithm:
                              description: '`alphabetical` or `creation_date`.'
                              type: string
                          type: object
                        type: array
                      indexTemplate:
                        properties:
                          mappingNestedObjectsLimit:
                            description: The maximum number of nested JSON objects
                              that a single document can contain across all nested
                              types. This limit helps to prevent out of memory errors
                              when a document contains too many nested objects. (Default
                              is 10000. Minimum value is `0`, maximum value is `100000`.)
                            type: number
                          numberOfReplicas:
                            description: The number of replicas each primary shard
                              has. (Minimum value is `0`, maximum value is `29`)
                            type: number
                          numberOfShards:
                            description: The number of primary shards that an index
                              should have. (Minimum value is `1`, maximum value is
                              `1024`.)
                            type: number
                        type: object
                      ipFilter:
                        description: Allow incoming connections from this list of
                          CIDR address block, e.g. `["10.20.0.0/16"]
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      keepIndexRefreshInterval:
                        description: Aiven automation resets index.refresh_interval
                          to default value for every index to be sure that indices
                          are always visible to search. If it doesn't fit your case,
                          you can disable this by setting up this flag to true.
                        type: boolean
                      maxIndexCount:
                        description: Maximum number of indexes to keep (Minimum value
                          is `0`)
                        type: number
                      recoveryBackupName:
                        description: ❗ Name of a backup to recover from
                        type: string
                      settings:
                        description: 'OpenSearch-specific settings, in json. e.g.`jsonencode({thread_pool_search_size:
                          64})`. Use `exo x get-dbaas-settings-opensearch` to get
                          a list of available settings.'
                        type: string
                      version:
                        description: ❗ OpenSearch major version (`exo dbaas type show
                          opensearch` for reference)
                        type: string
                    type: object
                  pg:
                    properties:
                      adminPasswordSecretRef:
                        description: A custom administrator account password (may
                          only be set at creation time).
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      pgSettings:
                        description: PostgreSQL configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pg` for reference).
                        type: string
                      pgbouncerSettings:
                        description: PgBouncer configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pgbouncer` for reference).
                        type: string
                      pglookoutSettings:
                        description: pglookout configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pglookout` for reference).
                        type: string
                      recoveryBackupTime:
                        description: ISO time of a backup to recover from. May only
                          be set at creation time.
                        type: string
                      sharedBuffersPercentage:
                        description: Percentage of total RAM that the database server
                          uses for shared memory buffers. Valid range is 20-60, which
                          corresponds to 20% - 60%. This setting adjusts the shared_buffers
                          configuration value.
                        type: number
                      timescaledbSettings:
                        description: TimescaleDB extension configuration settings
                          in JSON format (`exo dbaas type show pg --settings=timescaledb`
                          for reference).
                        type: string
                      variant:
                        description: PostgreSQL variant (`timescale` or `aiven`).
                          May only be set at creation time.
                        type: string
                      version:
                        description: PostgreSQL major version (`exo dbaas type show
                          pg` for reference; may only be set at creation time).
                        type: string
                      workMem:
                        description: Sets the maximum amount of memory to be used
                          by a query operation (such as a sort or hash table) before
                          writing to temporary disk files, in MB. Default is 1MB +
                          0.075% of total RAM (up to 32MB).
                        type: number
                    type: object
                  plan:
                    description: The plan of the database service (use the [Exoscale
                      CLI](https://github.com/exoscale/cli/) - `exo dbaas type show
                      <TYPE> --plans` - for reference).
                    type: string
                  terminationProtection:
                    description: The database service protection boolean flag against
                      termination/power-off.
                    type: boolean
                  type:
                    description: ❗ The type of the database service (`kafka`, `mysql`,
                      `opensearch`, `pg`, `valkey`, `grafana`).
                    type: string
                  valkey:
                    properties:
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      valkeySettings:
                        description: Valkey configuration settings in JSON format
                          (`exo dbaas type show valkey --settings=valkey` for reference).
                        type: string
                    type: object
                  zone:
                    description: ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/)
                      name.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.plan is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.plan)
                || (has(self.initProvider) && has(self.initProvider.plan))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
            - message: spec.forProvider.zone is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zone)
                || (has(self.initProvider) && has(self.initProvider.zone))'
          status:
            description: DBAASServiceStatus defines the observed state of DBAASService.
            properties:
              atProvider:
                properties:
                  createdAt:
                    description: The creation date of the database service.
                    type: string
                  diskSize:
                    description: The disk size of the database service.
                    type: number
                  grafana:
                    properties:
                      grafanaSettings:
                        description: Grafana configuration settings in JSON format
                          (`exo dbaas type show grafana --settings=grafana` for reference).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    type: object
                  id:
                    type: string
                  kafka:
                    properties:
                      enableCertAuth:
                        description: Enable certificate-based authentication method.
                        type: boolean
                      enableKafkaConnect:
                        description: Enable Kafka Connect.
                        type: boolean
                      enableKafkaRest:
                        description: Enable Kafka REST.
                        type: boolean
                      enableSaslAuth:
                        description: Enable SASL-based authentication method.
                        type: boolean
                      enableSchemaRegistry:
                        description: Enable Schema Registry.
                        type: boolean
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      kafkaConnectSettings:
                        description: Kafka Connect configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=kafka-connect`
                          for reference).
                        type: string
                      kafkaRestSettings:
                        description: Kafka REST configuration settings in JSON format
                          (`exo dbaas type show kafka --settings=kafka-rest` for reference).
                        type: string
                      kafkaSettings:
                        description: Kafka configuration settings in JSON format (`exo
                          dbaas type show kafka --settings=kafka` for reference).
                        type: string
                      schemaRegistrySettings:
                        description: Schema Registry configuration settings in JSON
                          format (`exo dbaas type show kafka --settings=schema-registry`
                          for reference)
                        type: string
                      version:
                        description: Kafka major version (`exo dbaas type show kafka`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  maintenanceDow:
                    description: The day of week to perform the automated database
                      service maintenance (`never`, `monday`, `tuesday`, `wednesday`,
                      `thursday`, `friday`, `saturday`, `sunday`).
                    type: string
                  maintenanceTime:
                    description: The time of day to perform the automated database
                      service maintenance (`HH:MM:SS`)
                    type: string
                  mysql:
                    properties:
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      mysqlSettings:
                        description: MySQL configuration settings in JSON format (`exo
                          dbaas type show mysql --settings=mysql` for reference).
                        type: string
                      version:
                        description: MySQL major version (`exo dbaas type show mysql`
                          for reference; may only be set at creation time).
                        type: string
                    type: object
                  name:
                    description: ❗ The name of the database service.
                    type: string
                  nodeCpus:
                    description: The number of CPUs of the database service.
                    type: number
                  nodeMemory:
                    description: The amount of memory of the database service.
                    type: number
                  nodes:
                    description: The number of nodes of the database service.
                    type: number
                  opensearch:
                    properties:
                      dashboards:
                        properties:
                          enabled:
                            description: 'Enable or disable OpenSearch Dashboards
                              (default: true).'
                            type: boolean
                          maxOldSpaceSize:
                            description: 'Limits the maximum amount of memory (in
                              MiB) the OpenSearch Dashboards process can use. This
                              sets the max_old_space_size option of the nodejs running
                              the OpenSearch Dashboards. Note: the memory reserved
                              by OpenSearch Dashboards is not available for OpenSearch.
                              (default: 128).'
                            type: number
                          requestTimeout:
                            description: 'Timeout in milliseconds for requests made
                              by OpenSearch Dashboards towards OpenSearch (default:
                              30000)'
                            type: number
                        type: object
                      forkFromService:
                        description: ❗ Service name
                        type: string
                      indexPattern:
                        items:
                          properties:
                            maxIndexCount:
                              description: Maximum number of indexes to keep before
                                deleting the oldest one (Minimum value is `0`)
                              type: number
                            pattern:
                              description: fnmatch pattern
                              type: string
                            sortingAlgorithm:
                              description: '`alphabetical` or `creation_date`.'
                              type: string
                          type: object
                        type: array
                      indexTemplate:
                        properties:
                          mappingNestedObjectsLimit:
                            description: The maximum number of nested JSON objects
                              that a single document can contain across all nested
                              types. This limit helps to prevent out of memory errors
                              when a document contains too many nested objects. (Default
                              is 10000. Minimum value is `0`, maximum value is `100000`.)
                            type: number
                          numberOfReplicas:
                            description: The number of replicas each primary shard
                              has. (Minimum value is `0`, maximum value is `29`)
                            type: number
                          numberOfShards:
                            description: The number of primary shards that an index
                              should have. (Minimum value is `1`, maximum value is
                              `1024`.)
                            type: number
                        type: object
                      ipFilter:
                        description: Allow incoming connections from this list of
                          CIDR address block, e.g. `["10.20.0.0/16"]
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      keepIndexRefreshInterval:
                        description: Aiven automation resets index.refresh_interval
                          to default value for every index to be sure that indices
                          are always visible to search. If it doesn't fit your case,
                          you can disable this by setting up this flag to true.
                        type: boolean
                      maxIndexCount:
                        description: Maximum number of indexes to keep (Minimum value
                          is `0`)
                        type: number
                      recoveryBackupName:
                        description: ❗ Name of a backup to recover from
                        type: string
                      settings:
                        description: 'OpenSearch-specific settings, in json. e.g.`jsonencode({thread_pool_search_size:
                          64})`. Use `exo x get-dbaas-settings-opensearch` to get
                          a list of available settings.'
                        type: string
                      version:
                        description: ❗ OpenSearch major version (`exo dbaas type show
                          opensearch` for reference)
                        type: string
                    type: object
                  pg:
                    properties:
                      adminUsername:
                        description: A custom administrator account username (may
                          only be set at creation time).
                        type: string
                      backupSchedule:
                        description: The automated backup schedule (`HH:MM`).
                        type: string
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      pgSettings:
                        description: PostgreSQL configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pg` for reference).
                        type: string
                      pgbouncerSettings:
                        description: PgBouncer configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pgbouncer` for reference).
                        type: string
                      pglookoutSettings:
                        description: pglookout configuration settings in JSON format
                          (`exo dbaas type show pg --settings=pglookout` for reference).
                        type: string
                      recoveryBackupTime:
                        description: ISO time of a backup to recover from. May only
                          be set at creation time.
                        type: string
                      sharedBuffersPercentage:
                        description: Percentage of total RAM that the database server
                          uses for shared memory buffers. Valid range is 20-60, which
                          corresponds to 20% - 60%. This setting adjusts the shared_buffers
                          configuration value.
                        type: number
                      timescaledbSettings:
                        description: TimescaleDB extension configuration settings
                          in JSON format (`exo dbaas type show pg --settings=timescaledb`
                          for reference).
                        type: string
                      variant:
                        description: PostgreSQL variant (`timescale` or `aiven`).
                          May only be set at creation time.
                        type: string
                      version:
                        description: PostgreSQL major version (`exo dbaas type show
                          pg` for reference; may only be set at creation time).
                        type: string
                      workMem:
                        description: Sets the maximum amount of memory to be used
                          by a query operation (such as a sort or hash table) before
                          writing to temporary disk files, in MB. Default is 1MB +
                          0.075% of total RAM (up to 32MB).
                        type: number
                    type: object
                  plan:
                    description: The plan of the database service (use the [Exoscale
                      CLI](https://github.com/exoscale/cli/) - `exo dbaas type show
                      <TYPE> --plans` - for reference).
                    type: string
                  state:
                    description: The current state of the database service.
                    type: string
                  terminationProtection:
                    description: The database service protection boolean flag against
                      termination/power-off.
                    type: boolean
                  type:
                    description: ❗ The type of the database service (`kafka`, `mysql`,
                      `opensearch`, `pg`, `valkey`, `grafana`).
                    type: string
                  updatedAt:
                    description: The date of the latest database service update.
                    type: string
                  valkey:
                    properties:
                      ipFilter:
                        description: A list of CIDR blocks to allow incoming connections
                          from.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      valkeySettings:
                        description: Valkey configuration settings in JSON format
                          (`exo dbaas type show valkey --settings=valkey` for reference).
                        type: string
                    type: object
                  zone:
                    description: ❗ The Exoscale [Zone](https://www.exoscale.com/datacenters/)
                      name.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
